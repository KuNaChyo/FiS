ERE_information_revolution = {
    ERE_computer_imports = {
		icon = GFX_decision_generic_industry

		available = {
			NOT = { has_country_flag = ERE_information_revolution_in_progress }
		}

		cost = 100
		days_remove = 30
		fire_only_once = yes

		modifier = {
			political_power_gain = -0.1
            civilian_factory_use = 3
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ERE_computer_imports"
			set_country_flag = ERE_information_revolution_in_progress
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ERE_computer_imports"
			set_country_flag = ERE_one_reform_end
			clr_country_flag = ERE_information_revolution_in_progress
			add_to_variable = {
				var = ERE_information_point
				value = 3
			}
		}
	}

    ERE_civilization_movement = {
		icon = GFX_decision_generic_industry

		available = {
			NOT = { has_country_flag = ERE_information_revolution_in_progress }
		}

		cost = 50
		days_remove = 75
		fire_only_once = yes

		modifier = {
			political_power_gain = -0.1
            consumer_goods_factor = 0.03
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ERE_civilization_movement"
			set_country_flag = ERE_information_revolution_in_progress
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ERE_civilization_movement"
			set_country_flag = ERE_two_reform_end
			clr_country_flag = ERE_information_revolution_in_progress
			add_to_variable = {
				var = ERE_information_point
				value = 10
			}
		}
	}

    ERE_overseas_instructor = {
		icon = GFX_decision_generic_decision

		available = {
			NOT = { has_country_flag = ERE_information_revolution_in_progress }
            check_variable = {
                ERE_information_point < 10
            }
		}

        modifier = {
			political_power_gain = -0.01
		}

		cost = 50
        days_remove = 14
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ERE_overseas_instructor"
			add_stability = -0.05
			add_to_variable = {
				var = ERE_information_point
				value = 5
			}
		}
	}

    ERE_end_the_revolution = {
		icon = GFX_decision_generic_operation

		available = {
			check_variable = {
				ERE_information_point > 30
			}	
			NOT = { has_country_flag = ERE_information_revolution_in_progress }
		}

		cost = 0
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ERE_end_the_revolution"
			set_country_flag = ERE_information_revolution_finished
			if = {
				limit = {
					check_variable = { ERE_information_point < 10 }
				}
                country_event = anastasia_russia.9
			}
			else_if = {
				limit = {
					check_variable = { ERE_information_point > 9 }
					check_variable = { ERE_information_point < 20 }
				}
				country_event = anastasia_russia.10
			}
			else_if = {
				limit = {
					check_variable = { ERE_information_point > 19 }
					check_variable = { ERE_information_point < 50 }
				}
				country_event = anastasia_russia.11
			}
			else_if = {
				limit = {
					check_variable = { ERE_information_point > 49 }
				}
				country_event = anastasia_russia.12
			}
		}
	}
}