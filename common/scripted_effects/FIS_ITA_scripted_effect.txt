ITA_reload_tree = {
	hidden_effect = {
		if = {
			limit = { has_focus_tree = FIS_ITA_swap }
			load_focus_tree = { tree = FIS_ITA keep_completed = yes }
		}
		else_if = {
			limit = { has_focus_tree = FIS_ITA }
			load_focus_tree = { tree = FIS_ITA_swap keep_completed = yes }
		}
	}
}